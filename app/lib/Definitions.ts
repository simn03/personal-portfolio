export type ItemType = {
    className?: string;
    image?: string;
    title: string;
    metadata: string[];
    url?: string;
    page?: string;
    description: string[];
}

export type MousePosition = {
    x: number;
    y: number;
}